import styled from 'styled-components';

export const HeaderStyle = styled.h1`
    font-weight: normal;
    font-size: 36px;
    line-height: 42px;
    color: #252525;

    text-shadow: -2px -4px 3px var(--white), 2px 4px 3px rgba(0, 0, 0, 0.25);
`;